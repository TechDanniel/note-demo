# Note Demo

这是一个最小可用的 VitePress 文档站点。

## 开始使用

- 本地开发：`pnpm docs:dev`
- 构建产物：`pnpm docs:build`
- 预览构建：`pnpm docs:preview`

## 写文档

- 新增页面：在 `docs/` 下创建 `xxx.md`
- 加入侧边栏：在 `docs/.vitepress/config.ts` 的 `themeConfig.sidebar` 里添加链接
